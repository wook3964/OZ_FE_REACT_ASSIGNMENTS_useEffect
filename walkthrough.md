# 작업 요약

## 변경 사항
1. **커밋 메시지 언어 변경**: 이제부터 모든 커밋 메시지와 도구 설명(Description)을 한국어로 작성합니다.
2. **useEffect 구현**: `src/App.jsx`에서 `useEffect`를 사용하여 다음 기능을 구현했습니다.
   - 컴포넌트 마운트 시 실행 로직 (`[]` 의존성 배열 추가)
   - 특정 값(`counter2`) 업데이트 시 실행 로직 (`[counter2]` 의존성 배열 추가)
   - 컴포넌트 언마운트 시 클린업 함수 추가

## 다음 단계
- 과제의 나머지 요구사항이 있는지 확인하고 추가 구현을 진행할 예정입니다.
